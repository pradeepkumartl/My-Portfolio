import { StructSkillsDirective } from './struct-skills.directive';

describe('StructSkillsDirective', () => {
  xit('should create an instance', () => {
    // const directive = new StructSkillsDirective();
    // expect(directive).toBeTruthy();
  });
});
