import { FieldValidatorDirective } from './field-validator.directive';

describe('FieldValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
